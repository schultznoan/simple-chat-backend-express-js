{
    "compilerOptions": {
        // Папка, в которую будут помещены скомпилированные файлы.
        "outDir": "dist",
        // Определяет корневую директорию (или корневые директории), относительно которой будет выполняться компиляция TypeScript.
        "rootDir": "./src",
        // Позволяют настраивать базовый путь для разрешения модулей и создавать псевдонимы путей.
        "baseUrl": "",
        // Задает версию ECMAScript, на которую будет транспилирован ваш код
        "target": "ES2018",
        // Указывает формат модулей (прим. commonjs - require(''), ESNext - import)
        "module": "commonjs",
        // Управляет разрешением модулей. Например, "node" означает использование Node.js-стиля разрешения модулей.
        "moduleResolution": "node",
        // Обеспечивает интероперабельность между CommonJS и ES6 модулями.
        "esModuleInterop": true,
        // Включает или отключает строгие проверки типов.
        "strict": false,
        // Создает файлы объявлений (.d.ts), которые описывают типы ваших модулей.
        "declaration": true,
        // Позволяет компилятору TypeScript пропускать проверку объявлений (declaration files) в файле .d.ts.
        "skipLibCheck": true,
        // Позволяет проверить, что имена файлов используются с согласованным регистром символов.
        "forceConsistentCasingInFileNames": true,
        // Включает поддержку экспериментальных декораторов.
        "experimentalDecorators": true,
        // Указывает, создавать ли файлы карт исходного кода (.js.map), которые могут быть полезными при отладке.
        "sourceMap": true,
        // Определяет, следует ли удалять комментарии из скомпилированного кода.
        "removeComments": true,
        // Включает поддержку итераций для целей, которые не поддерживают итераторы ECMAScript.
        "downlevelIteration": true,
        // Разрешает компиляцию js файлов
        // "allowJs":"true",
        // Определяет список библиотек, предоставляемых компилятору TypeScript.
        "lib": [
            "dom",
            "ES2021.String"
        ],
        // Указываете список типов (type declaration files), которые вы хотите включить в ваш проект.
        "types": [
            "@types/node"
        ],
        // Настройка сопоставлений путей (path mappings) для разрешения модулей TypeScript.
        "paths": {
            "~/*": [
                "./*"
            ],
            "@/*": [
                "./*"
            ]
        },
    },
    // Определяет список паттернов файлов, которые должны быть включены в процесс компиляции TypeScript.
    "include": [
        "./src"
    ],
    // Используется для указания паттернов файлов, которые должны быть исключены из процесса компиляции TypeScript.
    "exclude": [
        "node_modules/*",
        "dist"
    ]
}